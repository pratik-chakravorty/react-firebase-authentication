import React from 'react';

const LandingPage = () => (
    <div>
        <h1 className="hero-title">This is an example application that uses React and Firebase API's for authentication.</h1>
    </div>
)

export default LandingPage;